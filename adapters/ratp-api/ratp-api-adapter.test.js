const apiAdapter = require("./ratp-api-adapter");

describe("ratp-api adapter", () => {
  it("should return the all schedules", async () => {
    expect(1).toBe(1);
    // TODO
  });
  it("should fail on connectivity error", async () => {
    expect(1).toBe(1);
    // TODO
  });
});
